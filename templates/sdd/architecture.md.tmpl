# Architecture (설계 의도) — <PROJECT_NAME>

<!-- 산출물: docs/architecture.md (SDD). "어떻게 만들 것인가"의 설계 의도.
     주의: 이 파일은 SDD 설계서다. 하네스 "구조 스냅샷"(에이전트/스킬/훅 표)은 동일 경로를 step5/운영이
     부분 갱신하며 공존한다 — 설계 의도(상단)와 구조 스냅샷(하단)을 섹션으로 구분해 유지하라. -->

## 1. 시스템 개요
<핵심 컴포넌트와 책임. 1문단 + 간단한 텍스트 다이어그램.>
```
<예: client → API(app/) → service → repository → DB
              app/ ↔ infra/(EKS, Terraform) ↔ monitoring/(Prometheus)>
```

## 2. 컴포넌트 / 경계
| 컴포넌트 | 책임 | 위치 | 의존 |
|----------|------|------|------|
| <API> | <…> | `app/...` | <DB, …> |
| <Infra> | <…> | `infra/...` | <cloud> |

## 3. 데이터 모델 (핵심)
<주요 엔티티·관계. 스키마 변경은 add-db-migration 스킬로, forward-only.>

## 4. 핵심 결정 (요약 → 상세는 ADR)
- <결정 1> → `docs/adr/0001-*.md`
- <결정 2> → `docs/adr/0002-*.md`

## 5. 경계면 계약 (cross-boundary, QA 근거)
- API ↔ DTO: <…> | app env ↔ k8s/helm config: <…> | tf output ↔ app config: <…>

## 6. 위험·완화
- <기술 위험 → 완화>

---
<!-- 아래 스냅샷은 step5 종료 시 채우고 이후 부분 편집으로만 갱신(전체 재작성 금지). 규칙: 부분 편집만·표로 고정·구조 변경 시에만 갱신(헌법 Docs 섹션).
     표로 고정 — 행 단위 편집·diff 최소화. 상한 1화면, 길어지면 상세는 산출물 파일로. -->
## Harness 구조 스냅샷
> 최종 갱신: <YYYY-MM-DD> (작업: <한 줄>)

| 구분 | 항목 | 범위/시점 | 연결 |
|------|------|-----------|------|
| Agent | <backend-dev> | <app/> | <add-rest-endpoint 등> |
| Skill | <add-rest-endpoint> | <app, 트리거 로드> | <backend-dev> |
| Orchestrator | <feature-flow> | <생성-검증 패턴> | <팀 전체> |
| Hook | <safety/tdd-gate/circuit/observe> | <PreToolUse 등> | <settings.json> |
| Data | <_workspace/{NN}_{agent}_*> | <중간 산출물> | <qa 교차검증> |
