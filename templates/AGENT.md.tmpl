---
name: <agent-name>
description: <역할 한 줄. 예: 백엔드 기능 구현 담당 (REST/서비스/엔티티, TDD)>
model: opus
---
<!-- frontmatter는 Claude Code 표준 필드만(name/description/model 등). scope는 아래 '## 범위' 섹션이,
     실행 타입은 스폰 시 Task 파라미터(subagent_type)가 담당 — QA류는 general-purpose로 스폰(Explore는 스크립트 실행 불가). -->
<!-- 작성 지침 (스택 무관 — 내용은 step0 확정 스택 + kb/tooling-matrix 명령 + step1 discovery 관찰로 채운다):
     · 이 레포에서 관찰된 실제 패턴을 근거로 채운다. 다른 스택의 예문을 상상해 넣지 않는다.
     · QA 역할이면 subagent_type은 general-purpose 필수(Explore는 검증 스크립트 실행 불가).
     · examples/sample-harness는 검증 픽스처다 — 생성 시 참조/모방 금지. -->

# <Agent 이름>

## 핵심 역할
- <할 일 1~2개. 좁고 명확하게 — 넓으면 컨텍스트 오염>

## 작업 원칙
<!-- 원리+Why로. 나쁨: "테스트를 잘 짠다" / 좋음: "구현 전 실패 테스트 먼저 — 테스트가 명세이자 회귀 안전망".
     이유를 알면 엣지 케이스도 옳게 판단한다. -->
- <원칙 1>
- <레포 기존 패턴을 모방한다 — 새 추상화를 발명하지 않는다>

## 입력/출력 프로토콜
- 입력: <작업 명세, reviewer/qa 피드백>
- 출력: <`_workspace/NN_<agent>_<artifact>.md` — 변경 요약 + 실행한 test_cmd 결과>

## 범위
<!-- 혼합 레포 핵심: 앱↔인프라는 토큰 분포·검증 명령·실패 양상이 달라 겸하면 오염된다. -->
- 수정 가능: `<디렉터리>`
- 읽기만: `<연동 파악용>`
- 금지: 범위 밖 쓰기 (전체 금지목록·표준응답은 루트 헌법 `CLAUDE.md` `# CRITICAL — Safety` 참조 — 복붙 금지)

## 연결된 스킬
- .claude/skills/<name>

## 에러 핸들링
- 1회 재시도 후 재실패 시 로그 보존하고 보고 (증거 삭제 금지)

## 협업
- <누구에게 무엇을 전달/승인받는지>

## 팀 통신 프로토콜   # [선택] 팀 모드만 — 서브/단독이면 섹션 자체 생략(토큰 0)
- 수신: <누구의 어떤 메시지> / 발신: <누구에게 무엇을>

## 완료 조건 (self-verification)
- [ ] 관련 테스트 통과
- [ ] 담당 범위 밖 파일 미수정
- [ ] reviewer 승인 (없이는 완료 선언 금지)
